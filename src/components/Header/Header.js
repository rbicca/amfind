import classes from './Header.module.css';

const Header = () => {
    return(
        <div className={classes.base}>
            <h1>
                Report de Produção
            </h1>
        </div>
    );
}

export default Header;